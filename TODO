strsplit utiliser strchr
strsplit verifier la valeur de retour de get_word pour un NULL
strlcat gerer quon accede pas a la derniere case du buffer s1 quand i == size

printf BONUS :
couleur

binaire

precision .*
width *

dprintf
sprintf
vprintf
vaprintf
vdprintf
vsprintf

ordre des flags quelconque

list:

ft_list_insert doesnt work when it is on the last
void			ft_list_delete(t_list *list);

void			ft_list_resize(t_list *list, t_size_type n);
void			ft_list_resize_to(t_list *list, t_size_type n, void *val);

void			ft_list_assign(t_list *list, t_size_type n, const void *val);
